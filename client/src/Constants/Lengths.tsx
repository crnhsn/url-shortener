// todo - centralize these into env vars or other shared config
// so that both server and client can reference the same source of truth
export const MAX_URL_LENGTH = 2048;
export const MAX_ALIAS_LENGTH = 8;